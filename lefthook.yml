pre-commit:
  parallel: true
  commands:
    check:
      run: npm run check
    prettier:
      glob: '*'
      run: npx --no-install prettier {staged_files} --write --ignore-unknown && git add {staged_files}
